import { SmtFormItemDirective } from './smt-form-item.directive';

describe('SmtFormItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SmtFormItemDirective();
    expect(directive).toBeTruthy();
  });
});
