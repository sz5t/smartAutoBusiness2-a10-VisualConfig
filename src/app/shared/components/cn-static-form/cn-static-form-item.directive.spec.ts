import { CnStaticFormItemDirective } from './cn-static-form-item.directive';

describe('CnStaticFormItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CnStaticFormItemDirective();
    expect(directive).toBeTruthy();
  });
});
