import { CnAttributeItemDirective } from './cn-attribute-item.directive';

describe('CnAttributeItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CnAttributeItemDirective();
    expect(directive).toBeTruthy();
  });
});
