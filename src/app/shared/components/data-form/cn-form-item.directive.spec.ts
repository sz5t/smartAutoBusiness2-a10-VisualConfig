import { CnFormItemDirective } from './cn-form-item.directive';

describe('CnFormItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CnFormItemDirective();
    expect(directive).toBeTruthy();
  });
});
