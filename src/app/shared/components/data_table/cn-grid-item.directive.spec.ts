import { CnGridItemDirective } from './cn-grid-item.directive';

describe('CnGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CnGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
