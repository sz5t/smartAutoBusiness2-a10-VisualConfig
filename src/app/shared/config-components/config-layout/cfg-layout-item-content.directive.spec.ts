import { CfgLayoutItemContentDirective } from './cfg-layout-item-content.directive';

describe('CfgLayoutItemContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgLayoutItemContentDirective();
    expect(directive).toBeTruthy();
  });
});
