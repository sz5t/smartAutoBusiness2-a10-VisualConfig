import { CfgLControlDesignDirective } from './cfg-l-control-design.directive';

describe('CfgLControlDesignDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgLControlDesignDirective();
    expect(directive).toBeTruthy();
  });
});
