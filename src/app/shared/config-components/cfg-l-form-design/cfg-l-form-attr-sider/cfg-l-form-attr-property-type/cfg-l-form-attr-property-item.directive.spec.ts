import { CfgLFormAttrPropertyItemDirective } from './cfg-l-form-attr-property-item.directive';

describe('CfgLFormAttrPropertyItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgLFormAttrPropertyItemDirective();
    expect(directive).toBeTruthy();
  });
});
