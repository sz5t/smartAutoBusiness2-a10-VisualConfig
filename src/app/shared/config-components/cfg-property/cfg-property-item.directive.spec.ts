import { CfgPropertyItemDirective } from './cfg-property-item.directive';

describe('CfgPropertyItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgPropertyItemDirective();
    expect(directive).toBeTruthy();
  });
});
