import { CfgPageDesignLayoutItemDirective } from './cfg-page-design-layout-item.directive';

describe('CfgPageDesignLayoutItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgPageDesignLayoutItemDirective();
    expect(directive).toBeTruthy();
  });
});
