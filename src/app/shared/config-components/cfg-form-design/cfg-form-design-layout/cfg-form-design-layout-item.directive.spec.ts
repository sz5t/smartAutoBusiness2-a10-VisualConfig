import { CfgFormDesignLayoutItemDirective } from './cfg-form-design-layout-item.directive';

describe('CfgFormDesignLayoutItemDirective', () => {
  it('should create an instance', () => {
    const directive = new CfgFormDesignLayoutItemDirective();
    expect(directive).toBeTruthy();
  });
});
